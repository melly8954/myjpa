
### Mysql Create Database & User 명령어
```
CREATE TABLE users (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,    -- id: 기본키 및 자동 증가
    loginId VARCHAR(100) NOT NULL UNIQUE,    -- loginId: 사용자 로그인 ID (필수,고유)
    password VARCHAR(100) NOT NULL,          -- password: 사용자 비밀번호 (필수)
    name VARCHAR(100) NOT NULL,              -- name: 사용자 이름 (필수)
    nickname VARCHAR(30) NOT NULL UNIQUE,    -- nickname: 사용자 닉네임 (필수,고유)
    email VARCHAR(255) NOT NULL UNIQUE       -- email: 사용자 이메일 (필수,고유)
);

```